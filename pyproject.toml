[project]
name = "pt-new-companies"
version = "0.1.0"
description = "Track newly registered companies in Portugal"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.28.0",
    "streamlit>=1.30.0",
    "pandas>=2.0.0",
]

[dependency-groups]
dev = []
